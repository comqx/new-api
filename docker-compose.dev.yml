# Frontend Development - Backend built from local source
#
# 使用宿主机 PostgreSQL / Redis（127.0.0.1），不启动 compose 内的 pg/redis。
# 连接信息见项目根目录 .env；容器内通过 host.docker.internal 访问宿主机。
#
# Usage:
#   1. docker compose -f docker-compose.dev.yml up -d
#   2. cd web && bun install && bun run dev
#   3. Open http://localhost:3001 (Rsbuild dev server, API auto-proxied to :3000)
#
# Rebuild backend after Go code changes:
#   docker compose -f docker-compose.dev.yml up -d --build new-api
#
# Stop:
#   docker compose -f docker-compose.dev.yml down
#
# Reset data (宿主机 PG):
#   make reset-setup

services:
  new-api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: new-api-dev:local
    container_name: new-api-dev
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - dev_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - SQL_DSN=postgresql://root:123456@host.docker.internal:5432/new-api
      - REDIS_CONN_STRING=redis://:123@host.docker.internal:6379
      - TZ=Asia/Shanghai
      - BATCH_UPDATE_ENABLED=true
      # Enable only when accessing the dev backend through HTTPS. SESSION_COOKIE_TRUSTED_URL is required when true.
      # - SESSION_COOKIE_SECURE=true
      # - SESSION_COOKIE_TRUSTED_URL=https://example.com,https://admin.example.com

volumes:
  dev_data:
